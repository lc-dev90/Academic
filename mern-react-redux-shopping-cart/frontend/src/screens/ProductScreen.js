import React from "react";
import styled from "styled-components";

const ProductScreen = () => {
  return <ProductContainer>Hello from product screen</ProductContainer>;
};

export default ProductScreen;

const ProductContainer = styled.div``;
