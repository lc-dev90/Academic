import React from "react";
import styled from "styled-components";

const CartScreen = () => {
  return <CartContainer>Hello from cart screen</CartContainer>;
};

export default CartScreen;

const CartContainer = styled.div``;
