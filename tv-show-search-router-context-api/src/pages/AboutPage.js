import React from "react";
import styled from "styled-components";

const AboutPage = () => {
  return <About>This is the about page</About>;
};

export default AboutPage;

const About = styled.div``;
